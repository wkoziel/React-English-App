import React from 'react';
import Button from './Button';

const DeleteAccount = () => {
   return (
      <div>
         <Button label="Usuń" />
      </div>
   );
};

export default DeleteAccount;
