import React from 'react';
import Navbar from '../components/Navbar';
import { routes } from '../routes';

const Home = () => {
   return (
      <>
         <Navbar active={0} />
      </>
   );
};

export default Home;
